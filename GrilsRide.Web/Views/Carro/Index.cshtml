@model List<Carro>
@{
    ViewData["Title"] = "Rides";
}

@if (TempData["msgc"] != null)
{
    <div class="alert alert-success">
        @TempData["msgc"]
    </div>
}

<h1>Lista de Carros!</h1>

<table class="table table-hover">
    <tr>
        <th>Modelo do carro</th>
        <th>Placa</th>
        <th>Senha da Porta</th>
        <th>
            <form class="row row-cols-lg-auto g-3 mb-3 align-items-center">
                <div>
                    <input type="text" name="placa" placeholder="Digite a placa" class="form-control"/>
                </div>
                <div>
                    <button class="btn btn-outline-success" type="submit">Pesquisar</button>
                </div>
            </form>

        </th>
        
    </tr>

    @foreach (var v in Model)
    {
    <tr>
        <td>@v.ModeloCarro</td>
        <td>@v.Placa</td>
        <td>@v.SenhaPorta</td>
        <td> <a asp-action="Editar"  class="btn btn-outline-dark btn-sm" asp-route-id="@v.CarroId">Editar</a>
            

            <button onclick="IdCliente.value = @v.CarroId type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#exampleModal">
                 Cancelar
             </button>

        </td>
    </tr>
    }


</table>

<!-- Button trigger modal -->

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Cancelamento de coleta</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Deseja Realmente cancelar a corrida?
      </div>
      <div class="modal-footer">
        <form asp-action="remover">
            <input type="hidden" name="id" id="IdCliente"/>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Não</button>
            <button type="submit" class="btn btn-danger">Sim</button>
        </form>
      </div>
    </div>
  </div>
</div>